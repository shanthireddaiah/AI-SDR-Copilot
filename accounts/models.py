from django.db import models
from django.contrib.auth.models import User

# Using standard Django User model for registration, authentication, and session management.
# User fields: username, email, password, first_name, last_name, is_staff, date_joined
